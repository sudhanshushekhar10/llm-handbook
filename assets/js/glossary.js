/* =============================================================================
   glossary.js — the jargon, defined for someone who knows software, not ML.
   Powers hover tooltips (<span class="gloss" data-term="lora">…</span>)
   and the full Glossary page. `see` links to other terms by key.
   ============================================================================= */
window.HANDBOOK = window.HANDBOOK || {};

HANDBOOK.glossary = [
  { key:"token", term:"Token", aka:"subword unit",
    def:"The atomic unit a model reads and writes — usually a word-piece, not a whole word. Text is split into tokens before anything else happens. Think of it as the lexer's output: a stream of symbols the model operates on.",
    see:["tokenizer","bpe","context-window"] },
  { key:"tokenizer", term:"Tokenizer",
    def:"The component that converts text ↔ token IDs. Deterministic, fast, and learned once on a corpus. Closest software analogy: a lexer plus a fixed lookup table (the vocabulary).",
    see:["token","bpe","vocabulary"] },
  { key:"bpe", term:"Byte-Pair Encoding (BPE)",
    def:"A tokenizer-training algorithm that starts from characters/bytes and greedily merges the most frequent adjacent pair, over and over, until it has a vocabulary of the target size. Most GPT-style models use a byte-level variant.",
    see:["tokenizer","vocabulary"] },
  { key:"vocabulary", term:"Vocabulary",
    def:"The fixed set of all tokens a model knows (commonly 32k–256k). Each token maps to an integer ID and a row in the embedding table. Anything outside it is reconstructed from smaller pieces.",
    see:["token","embedding"] },
  { key:"embedding", term:"Embedding",
    def:"A learned dense vector (e.g. 4096 floats) that represents a token's meaning. Similar meanings land near each other in this space. Analogy: a hash that maps a symbol to a point in space where distance encodes similarity.",
    see:["vocabulary","embedding-model"] },
  { key:"context-window", term:"Context window",
    def:"The maximum number of tokens a model can attend to at once (its working memory). Everything — prompt, retrieved docs, conversation, output — must fit. Bigger windows cost more memory and compute.",
    see:["token","kv-cache","attention"] },

  { key:"transformer", term:"Transformer",
    def:"The neural-network architecture behind essentially every modern LLM. It processes all tokens in parallel and mixes information between them using attention. Introduced in 'Attention Is All You Need' (2017).",
    see:["attention","self-attention","positional-encoding"] },
  { key:"attention", term:"Attention",
    def:"A mechanism that lets each token pull in information from other relevant tokens, weighted by how relevant they are. The model learns what to 'look at'. It replaced recurrence and made training parallelizable.",
    see:["self-attention","multi-head","transformer"] },
  { key:"self-attention", term:"Self-attention",
    def:"Attention computed within a single sequence: every token compares itself (a query) against every other token (keys) and blends their values. The core operation repeated in every Transformer layer.",
    see:["attention","multi-head"] },
  { key:"multi-head", term:"Multi-head attention",
    def:"Running several attention operations in parallel, each in its own subspace ('head'), then concatenating. Different heads learn different relationships (syntax, coreference, position).",
    see:["attention","gqa"] },
  { key:"positional-encoding", term:"Positional encoding",
    def:"Information added to token representations so the model knows their order — attention itself is order-blind. Modern models often use RoPE (rotary positions).",
    see:["transformer","rope"] },

  { key:"parameter", term:"Parameter / weight",
    def:"A single learned number in the network. 'A 7B model' has ~7 billion of them. Training adjusts these; inference just reads them. They ARE the model — everything it 'knows' is encoded here.",
    see:["tensor","quantization"] },
  { key:"tensor", term:"Tensor",
    def:"A multi-dimensional array of numbers (the ML word for an n-D matrix). Weights, activations, and gradients are all tensors. PyTorch's core data type, much like NumPy's ndarray but GPU-aware.",
    see:["parameter","pytorch"] },
  { key:"logits", term:"Logits",
    def:"The raw, unnormalized scores the model outputs for every token in the vocabulary at each step. Passing them through softmax turns them into a probability distribution over the next token.",
    see:["softmax","temperature"] },
  { key:"softmax", term:"Softmax",
    def:"A function that turns a vector of logits into probabilities that sum to 1. The bigger a logit, the bigger its share. The final step before sampling the next token.",
    see:["logits","temperature"] },
  { key:"temperature", term:"Temperature",
    def:"A knob that flattens or sharpens the next-token distribution before sampling. Low (→0) = deterministic and focused; high (>1) = more random and creative. A core generation parameter.",
    see:["top-p","top-k","logits"] },
  { key:"top-k", term:"Top-k sampling",
    def:"Restrict sampling to the k most likely next tokens, renormalize, then sample. Prevents rare, weird tokens from sneaking in.",
    see:["top-p","temperature"] },
  { key:"top-p", term:"Top-p (nucleus) sampling",
    def:"Sample only from the smallest set of tokens whose probabilities sum to p (e.g. 0.9). Adapts the candidate pool to the model's confidence at each step.",
    see:["top-k","temperature"] },
  { key:"autoregressive", term:"Autoregressive generation",
    def:"Generating text one token at a time, feeding each output back in as input for the next step. Like a REPL that consumes its own output. This is why long outputs are slower.",
    see:["kv-cache","inference"] },

  { key:"pretraining", term:"Pretraining",
    def:"The first, expensive training phase: predict the next token over web-scale text. Produces a 'base model' that has absorbed language and world knowledge but isn't yet a helpful assistant.",
    see:["fine-tuning","scaling-laws"] },
  { key:"loss", term:"Loss",
    def:"A single number measuring how wrong the model's predictions are. Training minimizes it. For LLMs it's usually cross-entropy on the next token; its exponential is perplexity.",
    see:["perplexity","gradient-descent"] },
  { key:"perplexity", term:"Perplexity",
    def:"exp(loss). Intuitively, the model's average 'branching factor' — how many tokens it's effectively choosing between. Lower is better; a perplexity of 10 means it's about as unsure as a fair 10-sided die.",
    see:["loss"] },
  { key:"gradient-descent", term:"Gradient descent",
    def:"The optimization loop of all deep learning: compute the loss, find which direction each weight should move to reduce it (the gradient, via backpropagation), take a small step, repeat.",
    see:["backprop","optimizer","learning-rate"] },
  { key:"backprop", term:"Backpropagation",
    def:"The algorithm that computes the gradient of the loss with respect to every weight by applying the chain rule backwards through the network. Frameworks (PyTorch) do this automatically ('autograd').",
    see:["gradient-descent","pytorch"] },
  { key:"optimizer", term:"Optimizer (Adam / AdamW)",
    def:"The rule that turns gradients into weight updates. AdamW is the LLM default; it adapts the step size per-parameter and keeps two extra 'moment' buffers per weight — a big chunk of training memory.",
    see:["gradient-descent","learning-rate","mixed-precision"] },
  { key:"learning-rate", term:"Learning rate",
    def:"How big a step the optimizer takes. Too high → divergence; too low → glacial training. Usually scheduled: warm up, then decay. The single most important hyperparameter to get roughly right.",
    see:["optimizer"] },
  { key:"scaling-laws", term:"Scaling laws",
    def:"Empirical power-law relationships showing loss decreases predictably as you scale compute, data, and parameters. Chinchilla (2022) refined the optimal data-to-parameter ratio.",
    see:["pretraining","emergence"] },
  { key:"emergence", term:"Emergent abilities",
    def:"Capabilities (e.g. arithmetic, chain-of-thought) that are absent in small models and appear, sometimes abruptly, past a scale threshold. Debated, but influential.",
    see:["scaling-laws"] },

  { key:"fine-tuning", term:"Fine-tuning",
    def:"Continuing to train a pretrained model on a smaller, targeted dataset to specialize its behavior or style. Cheaper than pretraining; the main way teams adapt open models.",
    see:["sft","peft","lora","rag"] },
  { key:"sft", term:"Supervised fine-tuning (SFT)",
    def:"Fine-tuning on curated (prompt → ideal response) pairs. The first alignment step: it teaches a base model to follow instructions and adopt a chat format.",
    see:["fine-tuning","instruction-tuning","rlhf"] },
  { key:"instruction-tuning", term:"Instruction tuning",
    def:"SFT on a broad mix of tasks phrased as instructions, so the model generalizes to following instructions it hasn't seen. The step that turns GPT-3 into something usable.",
    see:["sft","chat-template"] },
  { key:"peft", term:"PEFT (Parameter-Efficient Fine-Tuning)",
    def:"A family of methods that train only a tiny fraction of new or selected parameters while freezing the rest. Drastically cuts memory and storage. LoRA is the most popular member; `peft` is also the Hugging Face library.",
    see:["lora","qlora","adapter"] },
  { key:"lora", term:"LoRA (Low-Rank Adaptation)",
    def:"Freeze the base weights; inject small trainable low-rank matrices (A·B) whose product is added to chosen weight matrices. You train <1% of parameters. Analogy: a git patch over a read-only base, not a fork.",
    see:["peft","qlora","rank"] },
  { key:"qlora", term:"QLoRA",
    def:"LoRA on top of a base model quantized to 4-bit, so the frozen weights barely use memory while you train the LoRA adapters. Lets you fine-tune very large models on a single consumer/cloud GPU.",
    see:["lora","quantization","int4"] },
  { key:"adapter", term:"Adapter",
    def:"A small trainable module inserted into a frozen network. The original PEFT approach (2019); LoRA is a low-rank, mergeable cousin that adds no inference latency.",
    see:["peft","lora"] },
  { key:"rank", term:"Rank (in LoRA)",
    def:"The inner dimension r of the low-rank update (e.g. 8, 16, 64). Higher rank = more capacity and more trainable parameters. Often paired with an 'alpha' scaling factor.",
    see:["lora"] },

  { key:"alignment", term:"Alignment",
    def:"Making a model's behavior match human intent and values — helpful, honest, harmless. Spans SFT, RLHF/DPO, and safety work. Turns a raw text predictor into an assistant.",
    see:["rlhf","dpo","sft"] },
  { key:"rlhf", term:"RLHF",
    def:"Reinforcement Learning from Human Feedback: train a reward model on human preference comparisons, then optimize the LLM to score well under it (usually with PPO). The recipe behind InstructGPT/ChatGPT.",
    see:["reward-model","ppo","dpo","alignment"] },
  { key:"reward-model", term:"Reward model",
    def:"A model trained to predict which of two responses a human would prefer, outputting a scalar score. Acts as a stand-in for human judgment during RL fine-tuning.",
    see:["rlhf","ppo"] },
  { key:"ppo", term:"PPO",
    def:"Proximal Policy Optimization — the RL algorithm classically used in RLHF to nudge the model toward higher reward without drifting too far from its starting point.",
    see:["rlhf","reward-model"] },
  { key:"dpo", term:"DPO (Direct Preference Optimization)",
    def:"A simpler alternative to RLHF: optimize directly on preference pairs with a classification-style loss, skipping the separate reward model and RL loop. Now a very common alignment method.",
    see:["rlhf","alignment"] },
  { key:"rlaif", term:"RLAIF / Constitutional AI",
    def:"Replacing much of the human feedback with AI feedback guided by a written set of principles (a 'constitution'). Scales alignment data generation. Introduced by Anthropic (2022).",
    see:["rlhf","alignment"] },

  { key:"quantization", term:"Quantization",
    def:"Storing/computing weights at lower numeric precision (e.g. 16-bit → 4-bit) to shrink memory and speed up inference, accepting a small quality loss. Analogy: lowering an image's bit-depth.",
    see:["fp16","int4","gptq","awq","gguf"] },
  { key:"fp16", term:"fp16 / bf16",
    def:"16-bit floating-point formats. bf16 keeps fp32's exponent range with less precision and is the modern training default; fp16 has more precision but a smaller range. Both halve memory vs fp32.",
    see:["quantization","mixed-precision"] },
  { key:"int4", term:"int8 / int4",
    def:"8-bit and 4-bit integer formats for weights. int4 cuts a 16-bit model's size by ~4×, making large models fit on small hardware. The basis of QLoRA and GGUF k-quants.",
    see:["quantization","qlora","gguf"] },
  { key:"gptq", term:"GPTQ",
    def:"A post-training quantization method that compresses to 3–4 bits one layer at a time while compensating for the error it introduces. Popular for GPU inference.",
    see:["quantization","awq"] },
  { key:"awq", term:"AWQ",
    def:"Activation-aware Weight Quantization: protect the small fraction of weights that matter most (judged by activation magnitudes) for strong 4-bit quality and speed.",
    see:["quantization","gptq"] },
  { key:"gguf", term:"GGUF",
    def:"The single-file model format used by llama.cpp (successor to GGML). Bundles weights + tokenizer + metadata, supports many CPU/GPU-friendly 'k-quant' levels (Q4_K_M, etc.). The format behind Ollama and local LLM apps.",
    see:["llama-cpp","quantization","int4"] },
  { key:"llama-cpp", term:"llama.cpp",
    def:"A C/C++ inference engine that runs quantized GGUF models efficiently on CPUs and consumer GPUs (including Apple Silicon via Metal). Powers a huge slice of local LLM usage.",
    see:["gguf","inference","ollama"] },

  { key:"inference", term:"Inference",
    def:"Running a trained model to produce outputs (as opposed to training it). For LLMs this is autoregressive decoding. The cost center in production: latency and throughput matter most here.",
    see:["autoregressive","kv-cache","serving"] },
  { key:"kv-cache", term:"KV cache",
    def:"A per-request cache of the keys and values already computed for previous tokens, so each new token doesn't recompute the whole sequence. Analogy: memoization. It dominates inference memory and grows with context length.",
    see:["attention","context-window","inference"] },
  { key:"serving", term:"Serving / continuous batching",
    def:"Infrastructure that runs a model for many concurrent requests. Continuous (in-flight) batching swaps requests in and out per token to keep the GPU busy — the key throughput trick in vLLM and TGI.",
    see:["vllm","kv-cache","inference"] },
  { key:"vllm", term:"vLLM",
    def:"A high-throughput inference/serving engine built around PagedAttention (a paged KV cache) and continuous batching. A common default for self-hosting LLMs on GPUs.",
    see:["serving","kv-cache","flashattention"] },
  { key:"flashattention", term:"FlashAttention",
    def:"An IO-aware, exact attention implementation that avoids writing the huge attention matrix to slow memory. Big speed and memory wins with identical results; standard in modern stacks.",
    see:["attention","serving"] },
  { key:"ollama", term:"Ollama",
    def:"A developer-friendly wrapper around llama.cpp that makes pulling and running quantized local models as easy as `ollama run llama3`. Great for local development.",
    see:["llama-cpp","gguf"] },

  { key:"moe", term:"Mixture-of-Experts (MoE)",
    def:"An architecture with many 'expert' sub-networks where a router activates only a few per token. Gives huge parameter counts with modest per-token compute (e.g. Mixtral, many frontier models).",
    see:["transformer","gqa"] },
  { key:"gqa", term:"GQA / MQA",
    def:"Grouped-Query / Multi-Query Attention: share key/value projections across attention heads to shrink the KV cache and speed up inference, with little quality loss. Used by Llama 2/3 and most recent models.",
    see:["multi-head","kv-cache"] },
  { key:"rope", term:"RoPE",
    def:"Rotary Position Embedding — encodes position by rotating query/key vectors. Extends gracefully to longer contexts (via interpolation/NTK scaling), which is why it's near-universal now.",
    see:["positional-encoding","context-window"] },

  { key:"mixed-precision", term:"Mixed precision",
    def:"Training with low-precision (bf16/fp16) math for speed/memory while keeping a few things in fp32 for stability. Standard practice; roughly doubles effective throughput.",
    see:["fp16","optimizer"] },
  { key:"gradient-accumulation", term:"Gradient accumulation",
    def:"Summing gradients over several small batches before updating, to simulate a large batch that wouldn't fit in memory. A staple trick when GPU memory is tight.",
    see:["optimizer","fsdp"] },
  { key:"fsdp", term:"FSDP / DeepSpeed ZeRO",
    def:"Techniques to train models too big for one GPU by sharding parameters, gradients, and optimizer state across many GPUs. The distributed-systems layer of large-scale training.",
    see:["mixed-precision","optimizer"] },
  { key:"checkpoint", term:"Checkpoint",
    def:"A saved snapshot of model weights (and sometimes optimizer state) at a point in training, so you can resume, evaluate, or deploy it. The 'build artifact' of training.",
    see:["pretraining","fine-tuning"] },

  { key:"huggingface", term:"Hugging Face",
    def:"The de-facto hub and toolmaker of open ML: the Hub hosts models/datasets (think GitHub for weights), and libraries like `transformers`, `datasets`, `peft`, and `accelerate` are the standard toolkit.",
    see:["transformers-lib","pytorch"] },
  { key:"transformers-lib", term:"transformers (library)",
    def:"Hugging Face's flagship Python library providing unified APIs to load, run, and train thousands of models. Usually your entry point: `AutoModelForCausalLM.from_pretrained(...)`.",
    see:["huggingface","pytorch"] },
  { key:"pytorch", term:"PyTorch",
    def:"The dominant deep-learning framework (Python + C++/CUDA). Provides tensors, autograd, and GPU acceleration. Most LLM code you'll read is PyTorch; JAX and TensorFlow are the main alternatives.",
    see:["tensor","cuda","backprop"] },
  { key:"cuda", term:"CUDA / GPU / VRAM",
    def:"GPUs do the massively-parallel matrix math LLMs need. CUDA is NVIDIA's programming layer; VRAM is the GPU's memory, the usual bottleneck. Apple Silicon uses Metal/MPS instead.",
    see:["pytorch","quantization","kv-cache"] },
  { key:"pydantic", term:"pydantic",
    def:"A Python data-validation library (not ML-specific). In LLM apps it defines/validates structured I/O — e.g. forcing a model's JSON output into a typed schema for tool calls and 'structured output'.",
    see:["structured-output"] },
  { key:"structured-output", term:"Structured output / function calling",
    def:"Constraining a model to emit machine-readable output (often JSON matching a schema) so software can consume it reliably — the bridge between fuzzy text and typed APIs.",
    see:["pydantic","agents"] },

  { key:"rag", term:"RAG (Retrieval-Augmented Generation)",
    def:"Fetch relevant documents at query time and put them in the prompt so the model answers from them. Analogy: open-book exam vs memorization. Often a better first choice than fine-tuning for fresh/private facts.",
    see:["embedding-model","vector-db","hallucination"] },
  { key:"embedding-model", term:"Embedding model",
    def:"A model that maps text to a single dense vector capturing meaning, used for semantic search and RAG retrieval. Different job from a generative LLM, though related.",
    see:["embedding","vector-db","rag"] },
  { key:"vector-db", term:"Vector database",
    def:"A store optimized for nearest-neighbor search over embeddings (FAISS, pgvector, Pinecone, etc.). The retrieval backend of most RAG systems.",
    see:["embedding-model","rag"] },
  { key:"hallucination", term:"Hallucination",
    def:"When a model states something fluent but false. A consequence of being a probabilistic next-token predictor, not a database. Mitigated (not eliminated) by RAG, grounding, and good prompting.",
    see:["rag","alignment"] },
  { key:"prompt", term:"Prompt / system prompt",
    def:"The input text conditioning a model's response. The system prompt is a special leading instruction that sets persona, rules, and format for the whole conversation.",
    see:["chat-template","zero-shot"] },
  { key:"chat-template", term:"Chat template",
    def:"The exact formatting (special tokens, role markers) that wraps user/assistant turns for a given model. Using the wrong template is a top cause of bad fine-tuning and inference results.",
    see:["sft","prompt"] },
  { key:"zero-shot", term:"Zero-shot / few-shot",
    def:"Zero-shot: ask with no examples. Few-shot: include a few worked examples in the prompt so the model imitates the pattern (in-context learning). GPT-3's headline capability.",
    see:["prompt","instruction-tuning"] },
  { key:"distillation", term:"Distillation",
    def:"Training a smaller 'student' model to mimic a larger 'teacher' (its outputs or probabilities), transferring much of the capability at a fraction of the size/cost.",
    see:["quantization","fine-tuning"] },
  { key:"agents", term:"Agents",
    def:"Systems where an LLM plans and calls tools/APIs in a loop to accomplish multi-step goals, observing results and deciding the next action. Built on structured output + tool use.",
    see:["structured-output","rag"] },
];

/* lookups */
HANDBOOK.glossaryByKey = {};
HANDBOOK.glossary.forEach(function (g) { HANDBOOK.glossaryByKey[g.key] = g; });
